Результат дня

Реализуем подключение LLM-модели (например, GPT-5 mini) к интерфейсу Telegram-бота для создания интеллектуального ассистента. Разберем, как работать с API OpenAI и BotFather, настроим «короткую память» диалога для сохранения контекста и научимся управлять ролями бота через системные промпты. Расширим возможности агента, добавив внешние функции (например, генерацию видео или изображений) и логирование стоимости запросов.

▶️ Время чтения и просмотра: ~ 35 минут

⏱ Время выполнения задания: ~ 40 минут

Теория на сегодня

1. Подключение LLM к Telegram-боту


В ближайших трёх уроках мы подытожим всё изученное касательно промптов, памяти, дополнительных инструментов, и так далее. В этом уроке мы будем подключать интерфейс в виде Telegram-бота к LLM-модели, чтобы мы могли с ней взаимодействовать.

Что такое LLM? ↘️
✅
LLM (Large Language Model, большая языковая модель) — это математический алгоритм, сутью которого является предсказание того, что пользователь хочет получить в ответе. 

LLM чаще всего используются как:

ИИ-ассистенты (агенты);
Чаты поддержки (обработка стандартных пользовательских запросов, не требующих оператора);
Генераторы текстов.
LLM имеют два вида памяти:

“Короткая” — память, с которой мы взаимодействуем в плане истории диалога. В ней содержится системный промпт, запросы и ответы модели (контекст диалога).
“Долгая” — данные, хранящиеся в базе данных, и прочая информация, которая не зависит от того, кто именно производит запрос.
💡
Помни, что контекстное окно модели является ограниченным. Также следует помнить, что контекст будет явно зависеть от того, какой будет написан промпт. 

⚠️
Необходимо различать понятия промпта и обычного запроса. Промпты являются более конкретными, и содержат контекст, в рамках которого LLM сразу выстраивает роли, характеризующие полноту, тип информации и стиль ответа. Финальный результат работы с ИИ прямо зависит от качественного промптинга модели. 

Всего существуют три главные роли: System (общий контекст и поведение), Assistant (отвечающая нейросеть) и User (пользователь, взаимодействующий с моделью). 

 

2. Практический кейс. Создание чат-бота Telegram

Подключение к API ↘️
Давайте ещё раз проговорим, где нам нужно брать данные для подключения Telegram-бота.

Токен для бота. В Telegram открываем диалог с BotFather — сервисом по созданию ботов. Можно пользоваться либо командами в диалоге, либо нажать кнопку Open и инициализировать приложение.

Нажимаем на Create New Bot.

Зададим боту произвольное название (например, zerotest1211) и свободное имя пользователя. Обязательно, чтобы в конце имени пользователя был постфикс bot. Можно также оставить произвольное описание. Жмём на Create Bot.

У нас появится уведомление о генерации API токена. Нажимаем Copy для его копирования. В любой момент можно нажать на Revoke для удаления данного токена.

Вставляем этот токен в файл .env нашего проекта, в переменную BOT_TOKEN.

Теперь получим токен OpenAI. Переходим на сайт платформы OpenAI. Заходим в аккаунт (можно через почту или аккаунт Google). Переходим в Settings → Billing. Убедись, что у тебя на счету есть некоторая сумма для осуществления запросов. Когда баланс достигнет нуля, наши запросы перестанут работать.
💡
Можешь не бояться, что OpenAI быстро потратит деньги на балансе аккаунта. Например, за всё время тестирования, мы на настоящий момент потратили всего 7 центов (ок. 7 руб.( 


Заходим в раздел API keys. Нажимаем Create new secret key. Вводим имя и выбираем тестовый проект. Нажимаем на кнопку Create secret key.

Скопируй токен. Будь внимателен, этот диалог появится один раз, в дальнейшем скопировать токен будет нельзя, и придётся создавать новый.

Вставь его в файл .env выше в качестве переменной OPENAI_API_KEY или аналогичной.
Заодно поменяем используемую модель. Перейди в Default project → Models. Например, возьмём модель GPT-5 mini. Копируем тег модели, нажав на кнопку правее её названия, и вставляем в файл .env как переменную среды OPENAI_MODEL.


💡
Обязательно сохрани изменения в файле .env. Отследить, сохранён ли файл, можно по наличию точки во вкладках с файлами в Cursor. 



 

3. Запуск бота Telegram и клиента OpenAI

Запуск бота Telegram и клиента OpenAI ↘️
После того, как мы получили необходимые токены, мы переходим в Cursor. Первое, что нам необходимо — это написать промпт. В нём мы должны указать, что мы делаем бота на основе aiogram и OpenAI API. Явно потребуем, чтобы бот обладал контекстной памятью, хранил системные промпты и последние сообщения пользователя, и имел несколько ролей. Ответ также должен содержать комментарии и разделы, для облегчения расширения.

✍️
Создай Telegram-бота на Python с использованием aiogram (v3 или выше) и OpenAI API.

✅ Требования:
1. Бот должен поддерживать память (контекст диалога) — хранить последние N сообщений пользователя и ассистента для каждого чата (например, в словаре или JSON-файле).
2. Подключение к OpenAI API (модель gpt-5 mini).
3. Хранить системные промпты (режимы) в отдельном JSON-файле `prompts.json`, где можно легко добавлять и редактировать режимы.
4. Возможность переключать текущий промпт через команду `/mode` — бот должен показывать список доступных режимов и позволять выбрать.
5. Команда `/reset` очищает память диалога.
6. Код должен быть чистым, структурированным и легко расширяемым.

⚙️ Пример структуры `prompts.json`:
{
  "default_prompt": "assistant",
  "prompts": {
    "assistant": {
      "name": "Обычный помощник",
      "description": "Дружелюбный ассистент для повседневных задач.",
      "system_prompt": "Ты — полезный и дружелюбный помощник. Отвечай кратко, понятно и по существу."
    },
    "developer": {
      "name": "Помощник разработчика",
      "description": "Помогает писать и объяснять код.",
      "system_prompt": "Ты — опытный разработчик. Пиши чистый, понятный код и объясняй решения простыми словами."
    },
    "writer": {
      "name": "Редактор текстов",
      "description": "Помогает писать статьи, посты и описания.",
      "system_prompt": "Ты — профессиональный писатель. Пиши живо, структурировано и без воды."
    },
    "teacher": {
      "name": "Учитель",
      "description": "Объясняет сложные вещи простыми словами.",
      "system_prompt": "Ты — терпеливый преподаватель. Объясняй понятия простыми, понятными словами и примерами."
    },
    "analyst": {
      "name": "Аналитик",
      "description": "Анализирует данные и помогает принимать решения.",
      "system_prompt": "Ты — аналитик. Делай логичные выводы, объясняй их и предлагай конкретные шаги."
    }
  }
}

💡 Функциональность:
- При старте бот загружает `prompts.json`.
- У каждого пользователя хранится выбранный режим и история сообщений.
- При получении сообщения бот добавляет его в память, формирует контекст (system_prompt + последние 5 сообщений), отправляет в OpenAI, и возвращает ответ.
- Код должен содержать комментарии и разделы (handlers, utils, config, main).

🎯 Итог:
Сгенерируй полностью рабочий пример проекта, включая файлы:
- `main.py`
- `config.py`
- `memory.py` (или `utils/memory.py`)
- `prompts.json`

Используй Python 3.10+ и `async`/`await`.

В результате мы, как и задавалось, сгенерировали файлы config.py с конфигурацией бота, memory.py с управлением памятью диалогов, добавлением сообщений, взаимодействием с файлами и получением истории, и main.py с логикой работы сервера (инициализация, обработчики команд пользователя и запросов Telegram и OpenAI). Логика работы в данном случае асинхронная, для возможности быстрого ответа сразу нескольким пользователям.
Также есть файлы prompts.json с нашими заданными промптами ролей и requirements.txt с требуемыми библиотеками. В качестве зависимостей мы задаём aiogram, OpenAI и python-dotenv для загрузки переменных среды.

Запускаем наше приложение. Для этого в терминале:

Вызываем виртуальное окружение python командой py -m venv venv.
Активируем виртуальное окружение командой .\venv\Scripts\activate .
Устанавливаем зависимости под виртуальным окружением командой pip install -r .\requirements.txt. Ждём установки зависимостей.
Запускаем бота python .\main.py.
Переходим в Telegram. Инициализируем бота, прописав в диалоге команду /start. Командой /mode мы можем менять режим (роль) агента. В дальнейшем, в рамках расширения, мы можем добавлять больше ролей для уточнения ответа.


💡
Обращайся к Cursor в случае возникновения ошибок. У нас, в рамках разработки тестового приложения, возникла ошибка, связанная с тем, что разные модели принимают на вход разные параметры (например, какие-то модели не принимают на вход параметр temperature). Внимательнее проверяй форматирование запросов. 


Наш помощник может отвечать на общие вопросы и запоминает контекст диалога.


 

4. Добавление функций внутрь ИИ-агента

Добавление функций внутрь ИИ-агента ↘️
Давайте добавим функцию, которая бы позволила нам генерировать видео. Заодно попросим LLM организовать более подробное логирование и обработчик стоимости запросов.

✍️
Добавь функцию для генерации видео на базе sora-2
Отдельная ветка для пользователя

Добавь логирование в бота(для отслеживания что происходит внутри)

Добавь вывод, сколько денег стоил каждый запрос(после сообщения должен выводить количество затраченных входных/выходных токенов + стоимость в рублях - курс получай с API ЦБРФ)

После каждой команды в терминале отображаются логи с нашими действиями и стоимость запроса (в USD). Отдельно осуществляется логирование ошибок.


💡
Обрати внимание, что с каждым новым запросом, у нас увеличивается количество входных токенов и, соответственно, стоимость запроса, так как мы дополнительно передаём историю сообщений для контекста. 


У нас появилась отдельная команда /video. Сейчас мы реализовали бота таким образом, что для генерации видео требуется переход в режим генерации видео через /mode.


⚠️
Генерировать промпт для Sora мы тоже будем через нейросеть, например, через ChatGPT. Sora лучше воспринимает промпты на английском языке, поэтому, откроем ChatGPT, зададим следующий промпт, переведём его на английский и подадим на вход Sora переведённый промпт. 

✍️
Промпт (на русском):
Короткое атмосферное видео в стиле вайб-кодинг: молодой разработчик сидит за ноутбуком в уютной комнате с мягким неоновым освещением (фиолетово-голубые тона), за окном дождь и отражения огней города. На столе кружка с кофе, механическая клавиатура с подсветкой. Камера плавно двигается, фокус на экране с кодом и лице программиста, создается ощущение уюта, вдохновения и сосредоточенности. Кадры плавные, кинематографичные, глубина резкости, лёгкое боке, реалистичное освещение. Длительность около 10 секунд.
Можно добавить (для лучшего вайба):
"саундтрек в стиле lo-fi chill coding beats" или "под мелодичный ambient-саундтрек".

Переведённая версия промпта:

✍️
A short cinematic "vibe coding" video: a young developer coding on a laptop in a cozy room with soft neon lighting (purple and blue tones). It's raining outside, reflections of city lights shimmer through the window. On the desk — a coffee mug and a mechanical keyboard with RGB lighting. The camera moves slowly, focusing on the screen and the programmer's face. The atmosphere feels calm, inspired, and focused. Realistic lighting, depth of field, smooth camera motion, slight bokeh. Duration ~10 seconds. Background music: chill lo-fi coding vibe.

Убедись в том, что нейросеть правильно определяет функцию генерации видео. В соответствии с референсом на сайте платформы OpenAI она должна называться openai.video.create(). Функция принимает на вход JSON объект с указанием модели и промпта. Другие параметры на вход не передаются.

<code class="language-JavaScript" style="white-space:pre-wrap;word-break:break-all">import OpenAI from 'openai';
const openai = new OpenAI();
let video = await openai.videos.create({
    model: 'sora-2',
    prompt: "A video of the words 'Thank you' in sparkling letters",
});
console.log('Video generation started: ', video);</code>
💡
При использовании ряда моделей может выскочить ошибка, связанная с проблемой авторизации, несмотря на то, что остальной код является полностью рабочим. 


<code class="language-JavaScript" style="white-space:pre-wrap;word-break:break-all"> PermissionDeniedError: Error code: 403 - {'error': ('message': 'Your organization must be verified to use the model `sora-2`. Please go to: https://platform.openai.com/se ttings/organization/general and click on Verify Organization. If you just verified, it can take up to 15 minutes for access to propagate.', 'type': 'invalid_request_error', 'par am': None, 'code': None}}</code>

Как и написано в коде ошибки, для того, чтобы исправить её, необходимо подтверждение личности (паспортных данных) через сервис Persona. Возможным решением также может быть использование посредников, например, ProxyAPI или GenAPI. но это будет несколько дороже. 

Вместо этого мы также можем сделать генерацию изображений, которая уже не будет требовать авторизации. К сожалению, модели OpenAI вроде gpt-image-1-mini также требую авторизацию. Поэтому, генерацию изображений реализуем на базе GenAPI. Пишем ИИ-агенту Cursor, что мы хотим сделать функцию генерацию фото с использованием данной модели. Вновь обращай особое внимание на то, какие параметры передаются на вход модели, и поддерживаются ли они ей.

В результате мы получаем масштабируемого бота, которого можно доработать другими функциями, большим количеством ролей и моделей, и так далее. LLM при этом будет являться ядром нашей системы.

💡
Напомним, что система получения результата несколько отличается от системы OpenAI. В данном случае запрос формируется непосредственно на сервере GenAPI и, по его выполнению, мы должны сделать второй запрос, который получает данные с сервера GenAPI. Вся процедура реализована Cursor автоматически. 


 

5. Итоги урока

В данном уроке мы сделали Telegram-бота, подключили его к нескольким API (OpenAI, GenAPI) для работы с ИИ и его функционалом, в том числе генерацией изображений. LLM во всём этом является ядром программы, поверх которого мы надстраиваем определённый функционал, память, интерфейс для взаимодействия.