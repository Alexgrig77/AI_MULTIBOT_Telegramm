# VPf09 AI Bot Telegram

Telegram-бот с интеграцией OpenAI через проксиapi для генерации текста, изображений и видео.

## Возможности

- 💬 Текстовые ответы с использованием LLM (GPT-4o-mini)
- 🖼️ Генерация изображений через `gpt-image-1-mini`
- 🎬 Генерация видео через `sora-2`
- 🧠 Контекстная память диалогов
- ⚙️ Переключение режимов работы (помощник, разработчик, писатель, учитель, аналитик)
- 💵 Отслеживание стоимости запросов в USD и RUB
- 📊 Логирование всех операций

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Alexgrig77/VPf09_AI_BOT_Telegramm.git
cd VPf09_AI_BOT_Telegramm
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
.\venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
copy .env.example .env
```

5. Заполните `.env` файл:
```
BOT_TOKEN=your_telegram_bot_token
PROXYAPI_BASE_URL=https://api.proxyapi.ru/openai/v1
OPENAI_API_KEY=your_proxyapi_key
OPENAI_MODEL=gpt-4o-mini
IMAGE_MODEL=gpt-image-1-mini
VIDEO_MODEL=sora-2
LOG_LEVEL=INFO
```

## Запуск

```bash
python main.py
```

## Использование

### Команды

- `/start` - Запуск бота и показ меню
- `/mode` - Выбор режима работы
- `/reset` - Очистка истории диалога
- `/image <описание>` - Генерация изображения
- `/video <описание>` - Генерация видео

### Режимы работы

1. **Обычный помощник** - Дружелюбный ассистент для повседневных задач
2. **Помощник разработчика** - Помогает писать и объяснять код
3. **Редактор текстов** - Помогает писать статьи, посты и описания
4. **Учитель** - Объясняет сложные вещи простыми словами
5. **Аналитик** - Анализирует данные и помогает принимать решения
6. **Генератор изображений** - Автоматическая генерация изображений по тексту
7. **Генератор видео** - Автоматическая генерация видео по тексту

### Режимы генерации

При выборе режима "Генератор изображений" или "Генератор видео" можно просто писать текстом описание, и бот автоматически сгенерирует изображение или видео.

## Структура проекта

```
VPf09_AI_BOT_Telegramm/
├── main.py              # Основной файл бота
├── config.py            # Конфигурация
├── memory.py            # Управление памятью диалогов
├── prompts.json         # Системные промпты (режимы)
├── requirements.txt     # Зависимости
├── .env.example         # Пример файла с переменными окружения
├── .gitignore           # Игнорируемые файлы
└── docs/
    └── TODO.md          # Список задач
```

## Технологии

- Python 3.10+
- aiogram 3.x - Telegram Bot API
- OpenAI Python SDK - для работы с OpenAI API через проксиapi
- python-dotenv - для загрузки переменных окружения

## Лицензия

MIT
